services:
  - type: web
    name: psiqmentor-v55
    runtime: python
    pythonVersion: "3.11.12"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    disk:
      name: psiqmentor-data
      mountPath: /data
      sizeGB: 1
